apiVersion: apps/v1
kind: Deployment
metadata:
  name: gitops-test
  namespace: gitops-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gitops-test
  template:
    metadata:
      labels:
        app: gitops-test
    spec:
      containers:
        - name: nginx
          image: nginx:alpine
          ports:
            - containerPort: 80
